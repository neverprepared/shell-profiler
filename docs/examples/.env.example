# Example .env file for workspace profiles
# Copy this to your profile directory as .env and customize as needed
#
# This file uses dotenv format (no 'export' keyword)
# It is loaded automatically by .envrc via: dotenv_if_exists .env
#
# This file should contain:
#   - Tool-specific path variables (GIT_CONFIG_GLOBAL, KUBECONFIG, etc.)
#   - Secrets and API tokens (these are gitignored)
#
# direnv stdlib commands (PATH_add, use, layout) go in .envrc instead

# ============================================================================
# GIT CONFIGURATION
# ============================================================================
# Point git to profile-specific configuration file
GIT_CONFIG_GLOBAL="$WORKSPACE_HOME/dotfiles/.gitconfig"

# ============================================================================
# SSH CONFIGURATION
# ============================================================================
# Use workspace-specific SSH config instead of $HOME/.ssh/config
GIT_SSH_COMMAND="ssh -F $WORKSPACE_HOME/dotfiles/.ssh/config"

# ============================================================================
# XDG BASE DIRECTORY SPECIFICATION
# ============================================================================
# Point all XDG-compliant tools to workspace-specific config
# Tools like neovim, tmux, bat, ripgrep will use profile-specific configs
XDG_CONFIG_HOME="$WORKSPACE_HOME/dotfiles/.config"

# ============================================================================
# SSH AGENT
# ============================================================================
# Point to your SSH agent socket
# Example: 1Password SSH agent (macOS)
SSH_AUTH_SOCK="$HOME/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"
# Example: Default ssh-agent
# SSH_AUTH_SOCK="$HOME/.ssh/agent.sock"

# ============================================================================
# KUBERNETES / DOCKER
# ============================================================================

# Kubernetes
KUBECONFIG="$WORKSPACE_HOME/dotfiles/.kube/config"

# Docker
# DOCKER_CONFIG="$WORKSPACE_HOME/dotfiles/.docker"

# ============================================================================
# CLOUD PROVIDER CONFIGURATIONS
# ============================================================================

# AWS
# AWS_PROFILE="my-profile"
# AWS_DEFAULT_REGION="us-east-1"
# AWS_CONFIG_FILE="$WORKSPACE_HOME/dotfiles/.aws/config"
# AWS_SHARED_CREDENTIALS_FILE="$WORKSPACE_HOME/dotfiles/.aws/credentials"

# Google Cloud
# CLOUDSDK_CONFIG="$WORKSPACE_HOME/dotfiles/.config/gcloud"
# GOOGLE_APPLICATION_CREDENTIALS="$WORKSPACE_HOME/dotfiles/gcloud-key.json"

# Azure
# AZURE_CONFIG_DIR="$WORKSPACE_HOME/dotfiles/.azure"

# ============================================================================
# LANGUAGE-SPECIFIC CONFIGURATIONS
# ============================================================================
# Note: Version management commands (use node, layout python) go in .envrc
# Path variables and config file pointers go here

# Node.js / NPM
# NPM_CONFIG_USERCONFIG="$WORKSPACE_HOME/dotfiles/.npmrc"
# NODE_ENV="development"

# Python
# PYTHONPATH="$WORKSPACE_HOME/lib"
# VIRTUAL_ENV="$WORKSPACE_HOME/.venv"

# Ruby
# GEM_HOME="$WORKSPACE_HOME/.gems"

# Go
# GOPATH="$WORKSPACE_HOME/go"

# Rust
# CARGO_HOME="$WORKSPACE_HOME/.cargo"

# ============================================================================
# EDITOR AND TOOL PREFERENCES
# ============================================================================

# Editor preferences
# EDITOR="vim"
# VISUAL="code"

# Pager preferences
# PAGER="less"
# LESS="-R"

# ============================================================================
# PROJECT-SPECIFIC VARIABLES
# ============================================================================

# Database connections
# DATABASE_URL="postgresql://localhost:5432/mydb"
# REDIS_URL="redis://localhost:6379"

# Application configuration
# APP_ENV="development"
# LOG_LEVEL="debug"
# PORT="3000"

# ============================================================================
# SECRETS (DO NOT COMMIT - this file should be in .gitignore)
# ============================================================================

# API keys and tokens
# API_KEY="your-api-key-here"
# SECRET_KEY="your-secret-key-here"
# GITHUB_TOKEN="ghp_..."
# AWS_ACCESS_KEY_ID="AKIA..."
# AWS_SECRET_ACCESS_KEY="..."
